An appimage applications can be run directly without installation on modern
 Linux desktop, but cannnot be searched and launched like other applications
installed normally. To fix this inconvenience, we can create `desktop` file on
the folder `~/.local/share/applications`.

There is one example: `configuration/cherry-studio.desktop`